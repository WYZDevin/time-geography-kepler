services:
  backend:
    build: app/back-end
    ports:
      - "8000:8000"

  frontend:
    build:
      context: app/front-end
      args:
        VITE_BACKEND_URL: http://localhost:8000
    ports:
      - "5173:80"
    depends_on:
      - backend
