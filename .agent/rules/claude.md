---
trigger: always_on
---

# Monorepo Navigation

This repo has two sub-projects, each with its own `CLAUDE.md`:

- **`app/front-end/CLAUDE.md`** — React + deck.gl frontend (TypeScript, Vite)
- **`app/back-end/CLAUDE.md`** — Flask API backend (Python, geopandas)

Read the relevant sub-project's `CLAUDE.md` before making changes. The root `CLAUDE.md` covers repo structure, dev commands, and cross-codebase rules.
